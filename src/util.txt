//request obtem dados da requisição que está acontecendo
//response devolve uma resposta para quem está consumindo

//Tipos de parâmetros
//Request Param
//Query param

/*
app.get("/users", (request, response) => {
  const search = String(request.query.search);

  const filteredUsers = search
    ? users.filter((user) => user.includes(search))
    : users;

  response.json(filteredUsers);
});
*/

/*
app.get("/users/:id", (request, response) => {
  const id = Number(request.params.id);

  const user = users[id];

  return response.json(user);
});
*/

/*
app.post("/users", (request, response) => {
  const data = request.body;

  console.log(data);

  const user = {
    name: data.name,
    email: data.email,
  };

  return response.json(user);
});
*/

//Node só entende JavaScript
//Para entender TypeScript, precisa instalar npm install ts-node -d

//npm instala pacotes
//npx executa um pacote instalado

//npm install typescript -d

//npx tsc --init

//npx ts-node src/server.ts

//npm install ts-node-dev -D

//npm install knex

//Migrations = histórico do banco de dados

//npm knex migrate:latest --knexfile knexfile.ts migrate:latest